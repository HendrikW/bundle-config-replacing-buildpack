#!/bin/sh
# bin/compile <build-dir> <cache-dir>

echo "-----> Removing cached .bundle/config"

if [ -f $2/.bundle/config ]; then 
  rm $2/.bundle/config
fi

echo "       Copying over new .bundle/config"
cp -v -R $1/.heroku_bundle $1/.bundle
sed -i "s,/app,$1,g" $1/.bundle/config
